---
layout: default
title: 面经实录：阿里 AI Agent 开发一面
description: 阿里 Agent 开发岗一面 10 道真题，从架构设计到 kernel 优化全覆盖
eyebrow: Agent 面试通关 / 08
---

# 面经实录：阿里 AI Agent 开发一面

前 7 篇按考察维度拆解，这篇换个视角——**完整还原一场真实面试的问题流**。阿里这场一面的特点是覆盖面极广：从 Agent 架构设计一路问到 kernel 级优化，中间穿插 RAG、数据工程、训练方法。10 道题横跨“会不会做 Agent”和“底层功夫扎不扎实”两条线。

> 来源：阿里 AI Agent 开发岗一面，2026.3.29

---

## Q1：Agent 的架构设计？

**新手答**：“用大模型接工具就行。”

**高手答**：

一个完整的 Agent 不是单独一个大模型就能跑，核心拆成四层：

1. **任务入口层**：负责接收用户问题和上下文
2. **决策层**：意图识别、任务拆解、规划、工具选择
3. **执行层**：真正去调工具、查知识库、访问服务
4. **记忆与状态层**：维护多轮上下文、历史执行结果和中间变量

如果做得再工程化一点，通常还要加一个**校验层**——模型规划出来的步骤不一定对，工具参数也可能填错，所以在执行前后都要做检查：参数合法性校验、工具返回结构校验、结果一致性校验。

Agent 正确的地方不是“能不能想”，而是“想完能不能稳定执行”。

**考察点**：系统设计能力。面试官想看你脑子里有没有分层架构的概念，而不是把 Agent 等同于“大模型 + Prompt”。

---

## Q2：RAG 的检索如何实现？

**新手答**：“用向量数据库做相似度搜索。”

**高手答**：

RAG 检索分**离线**和**在线**两部分。

离线侧负责文档清洗、切片、去重、embedding 计算和向量入库。在线侧的流程是：

1. 用 query 编码成向量，去向量库做召回
2. 结合 BM25 或关键词检索做**混合召回**
3. 用 rerank 模型重排
4. 把最相关的证据拼接给大模型生成答案

```python
# 一个简化版的 RAG 检索流程
query_vec = embed(query)
candidates = vector_db.search(query_vec)
bm25_docs = bm25.search(query)
merged = merge_and_dedup(candidates, bm25_docs)
reranked = reranker.rank(query, merged)
context = "\n".join(doc.text for doc in reranked[:top_k])
answer = llm.generate(query, context)
```

切片策略直接影响效果：块太大召回不准，块太小上下文断裂。通常用**滑窗切割 + overlap**，对长文档还会给每个 chunk 带上标题、章节路径、来源元信息，提升召回相关性。最终效果不只看向量检索本身，还取决于 chunk 设计、召回策略和重排质量。

**考察点**：工程落地能力。面试官不想听“向量数据库”四个字，想听的是离线在线怎么分、混合召回怎么做、chunk 怎么切。

---

## Q3：预训练数据清洗方法？

**新手答**：“去重，过滤脏数据。”

**高手答**：

预训练数据清洗要解决三类问题：

| 类型 | 典型表现 |
|------|---------|
| 脏数据 | 乱码、HTML 残留、脚本片段、异常符号、错误编码 |
| 重复数据 | 完全重复、近重复 |
| 低质量数据 | 广告、灌水、模板文、机器翻译残片、拼接错乱文本 |

处理流程分三步递进：

1. **规则清洗**：去标签、过滤控制字符、长度约束、语言检测
2. **质量过滤**：困惑度过滤、分类器识别垃圾文本、关键词规则
3. **去重**：MinHash、SimHash、LSH 做近重复检测

真正影响模型上限的，很多时候不是模型结构，而是预训练数据质量。

**考察点**：数据工程基本功。这道题考的是你有没有处理过大规模数据的经验，能不能按“规则→统计→算法”的层次把清洗逻辑讲清楚。

---

## Q4：给定时间序列，如何用机器学习筛选重要特征，再基于规则建模？

**新手答**：“提取特征，训练模型，看 feature importance。”

**高手答**：

先把原始序列变成可学习的特征。常用的几类：

```text
统计特征：均值、方差、最大最小值
趋势特征：移动平均、环比
波动特征：标准差、变异系数
周期特征：傅里叶分量、周期自相关
滞后特征：lag 值、rolling lag
```

然后用 XGBoost / LightGBM / 随机森林训练，输出的关键因子整理成多条规则做线上解释。

这样做的好处是**既保留了机器学习筛特征的能力，也兼顾了规则系统的稳定和可解释**。线上跑的是规则，但规则背后的因子是机器学习选出来的——兼顾了性能和可控性。

**考察点**：特征工程 + 工程思维。面试官考的不是你会不会用 XGBoost，而是你能不能把 ML 和规则系统结合起来，满足线上可解释性要求。

---

## Q5：Agent 工具调用怎么训练？训练集该包含什么？数据怎么来？

**新手答**：“找一些工具调用的例子做微调。”

**高手答**：

训练集至少要覆盖**三类样本**：

1. **该调用工具的**——正样本
2. **不该调用工具的**——只教“会调用”不够，还得教“什么时候不要调”
3. **多工具串并联调用的**——复杂场景下的编排能力

数据来源一般有**四种**：

| 来源 | 做法 | 特点 |
|------|------|------|
| 人工标注轨迹 | 问题 → 思考过程 → 工具选择 → 参数 → 结果，整合成监督样本 | 质量高，成本大 |
| 线上日志回放 | 从高质量人工操作或已有系统中抽取工具调用链 | 真实，但需清洗 |
| 规则合成数据 | 用模板生成标准调用样本 | 量大，但多样性差 |
| 模型自举 | 用强模型生成轨迹，再人工检验修正 | 扩量快，需质检 |

真正关键的是**负样本和边界样本**——参数缺失、工具返回空、多个工具都可用但优先级不一……这些不补，模型上线很容易乱调。

**考察点**：Agent 训练的实操经验。面试官想确认你不只是调 API，还理解模型是怎么学会使用工具的，以及数据从哪来。

---

## Q6：多工具场景下的调度策略？

**新手答**：“模型自己选。”

**高手答**：

多工具场景的核心问题是**选哪个、按什么顺序、并行还是串行**。

1. **意图路由前置**：用户请求先经过轻量分类器或规则，判断需要哪类工具，不让大模型在全量工具里大海捞针
2. **依赖分析决定编排**：分析工具之间的输入输出依赖关系——无依赖的并行执行，有依赖的串行编排，减少总耗时
3. **优先级与降级**：多个工具都能用时，按可靠性、延迟、成本排优先级；主工具超时自动降级到备选
4. **结果合并与冲突处理**：多工具返回结果可能矛盾，需要合并策略——取最新的、取置信度最高的、或让模型做最终判断

调度策略不是模型能力问题，是**工程编排问题**——模型负责决策“做什么”，编排层负责“怎么执行”。

**考察点**：复杂系统的编排能力。面试官考的是你能不能把多工具协作从“模型自己想办法”变成一个可控的工程流程。

---

## Q7：构造数据集遇到过什么难点，怎么解决？

**新手答**：“数据不够，就多标一些。”

**高手答**：

最大的问题不是数据不够，而是**数据不真实**。

人工构造的数据常常太标准——用户表达很整齐，参数给得很全，工具永远成功。结果上线之后用户一句话没说全，模型就不会了。

三个核心难点和解法：

**难点一：数据不真实**
→ 把真实线上 query 引进来，按意图、复杂度、缺参情况、歧义情况分桶，然后做针对性补齐

**难点二：标注不一致**
→ 同一种问题不同人可能给出不同工具路径，所以需要**先统一 schema 和决策标准**，再开始标注

**难点三：长尾样本太少**
→ 靠模板改写、对抗生成和人工补充边界案例，把最容易出事故的地方优先补上

**考察点**：实战经验。这是纯经验题，面试官想听你真实踩过的坑，不是教科书上的方法论。能说出“数据不真实”比“数据不够”深一个层次。

---

## Q8：LoRA 和全参数微调的区别？

**新手答**：“LoRA 省显存，全参数效果好。”

**高手答**：

| 维度 | LoRA | 全参数微调 |
|------|------|-----------|
| 原理 | 在原始权重旁插入低秩矩阵，只训练新增参数 | 直接更新所有参数 |
| 参数量 | 通常只有原模型的 0.1%–1% | 100% |
| 显存 | 小，适合消费级 GPU | 大，需要多卡 |
| 训练速度 | 快 | 慢 |
| 任务切换 | 方便，换 adapter 即可 | 每个任务一个完整模型 |
| 适用场景 | 领域适配、指令跟随、工具调用格式对齐 | 需要深度改变模型行为的场景 |

LoRA 的核心优势不只是“省显存”，而是**方便任务切换和多租户部署**——同一个基座模型挂不同 adapter，就能服务不同场景。全参数微调自由度更大，但代价也更高。

**考察点**：训练方法论。面试官考的不是定义，而是你能不能说出“什么场景该用哪个”以及工程上的考量。

---

## Q9：DPO、PPO、GRPO？

**新手答**：“都是对齐方法，PPO 用 RL，DPO 不用。”

**高手答**：

三者的本质区别不在公式，在于**依赖的数据形态和工程复杂度**：

**PPO**：强化学习式对齐。需要 reward model 对输出打分，再根据奖励做策略更新。灵活，但训练链路长、超参数敏感、稳定性相对差。

**DPO**：把偏好学习直接写成监督式目标。用 chosen / rejected 成对数据直接优化策略，不显式采样长轨迹，也不显式训练 RL policy update。工程上更简单。

**GRPO**：把偏好从 pair 扩展到 group。不再只比较一对答案，而是利用一组候选之间的相对优劣关系做优化。能更充分利用多样候选信息，在排序、多样本比较和组内类比奖励场景更自然。

```text
PPO:  query → model → output → reward model → policy update
DPO:  query → chosen/rejected pair → 直接优化
GRPO: query → N 个候选 → 组内相对排序 → 优化
```

三者最大的区别不只是公式，而是它们依赖的数据形态、训练稳定性和适用范围不一样。

**考察点**：对齐方法的理解深度。面试官想看你是只记了名字，还是理解了它们在数据需求和工程实现上的本质差异。

---

## Q10：kernel 级别的优化，比如用 CUTE DSL 或手写 CUDA 做 fusion？

**新手答**：“就是把多个算子合成一个，减少开销。”

**高手答**：

kernel 级别优化的核心目标是**减少访存开销、减少 kernel launch 次数、提高并行利用率**。很多模型推理慢，不一定是算力不够，更多是 memory bound——数据搬运的时间比计算时间还长。

算子融合的思路：

1. **纵向融合**：把连续的 elementwise 操作（如 LayerNorm + Dropout + Add）合成一个 kernel，减少中间结果写回显存的次数
2. **横向融合**：把多个独立但可以并行的小 kernel 合并，减少 launch overhead
3. **访存优化**：利用 shared memory、寄存器缓存，减少 global memory 的访问次数

工具链的选择：手写 CUDA 最灵活但开发成本高；Triton 降低了门槛但灵活性有限；CUTE DSL 在特定场景（如矩阵运算）下能兼顾性能和开发效率。选哪个取决于**场景对性能的极致要求程度和团队的工程能力**。

**考察点**：底层功夫。Agent 岗位问这个，是在考察你的技术深度——不只是会调 API，还理解推理性能瓶颈在哪。

---

## 这场面试的整体特点

阿里这场一面的考察逻辑非常清晰——**从上往下穿透**：

```text
应用层：Agent 架构设计（Q1）、RAG 检索（Q2）
数据层：预训练清洗（Q3）、特征工程（Q4）、训练数据构造（Q5-Q7）
模型层：微调方法（Q8）、对齐方法（Q9）
系统层：kernel 优化（Q10）
```

10 道题覆盖了 Agent 开发的完整技术栈。面试官不只是在考“你会不会做 Agent”，更在考**“你的技术纵深够不够”**。只懂上层调用不够，还得理解数据怎么来、模型怎么训、底层怎么优化。

这也是 Agent 岗位和纯应用开发岗位的核心区别：Agent 开发需要的是**全栈 AI 工程能力**，不是只会写 Prompt。

---

返回模块首页：

- [Agent 面试通关：七大考察维度 + 面经实录](../index.html)
